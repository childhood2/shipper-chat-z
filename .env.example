# Auth (NextAuth)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-random-secret-with-openssl-rand-base64-32

# Google OAuth (optional — set both to enable "Continue with Google" on login and register)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# SQLite (required). Path relative to project root.
DATABASE_URL="file:./prisma/dev.db"
